# JEV-Orchestrator v3.0.0 — archived snapshot

This directory preserves a historical v3 implementation for traceability. It
is not the current supported runtime; use the repository root, version 4.0.0.
This curated copy has local account labels and machine-specific paths removed.
Its original behavior and tests are not a substitute for v4's security and
budget policy.
